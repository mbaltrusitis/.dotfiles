#!/usr/bin/env bash

veracrypt -t -k "" --pim=0 --protect-hidden=no "$HOME/.dotfiles/enc/vol.vc" "$HOME/enc-vol"

if [ -f "$HOME/enc-vol/ssh-keys/id_ed25519" ]; then
	ssh-add -k "$HOME/enc-vol/ssh-keys/id_ed25519"
fi
